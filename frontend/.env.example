# Base URL of the FastAPI backend. Copy this file to .env and adjust if the
# backend isn't running on localhost:8000 (e.g. pointing a laptop at a
# server on the same wifi network).
VITE_API_BASE=http://localhost:8000
